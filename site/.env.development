# 扩展 ID（Chrome：chrome://extensions → 本扩展 → ID）
# - 上架 Web Store 后 ID 固定
# - 本地「加载已解压」时一般随扩展目录路径稳定；换目录/换机后若变更，请同步改此处
# 配置后站点优先使用本变量，无需在页面再输入
VITE_EXTENSION_ID=mnmmhipopfnlfkpkknkgoofomclkiikf

# 可选：批量 Graph 每步间隔（毫秒），覆盖 config/fbControlBatch.json；未设置则用 JSON
# VITE_FB_BATCH_STEP_DELAY_MS=300
